#include<stdio.h>
#include<math.h>

int main()
{
  const int sample_rate = 500;
  
  while(true){
    for (i = 0; i < sample_rate; i++){
      for (j = 0; j < run_time[i]; j++){
      }
      usleep(sleep_time[i]);
    }
  }
  return 0;
}
